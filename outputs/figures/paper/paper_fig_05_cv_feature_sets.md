# Figure 5 — LOOCV binary performance by feature set

Best model per feature subset (BBS-only, Mini-BEST-only, FES-only, Combined). Bars: accuracy, balanced accuracy, macro-F1. Combined-model selection for deployment: best macro-F1 among classifiers on Combined features (binary best: Random Forest).
